<% content_for(:breadcrumb) do %>
  <%= breadcrumb('Account' =>'#') %>
<% end %>

<div>

  <%= pageheader('Account Information') %>

  <table>
    <tr>
      <th>Name</th>
      <td><%= @user.name %></td>
    </tr>
    <tr>
      <th>Email</th>
      <td><%= @user.email %></td>
    </tr>
  </table>

</div>
